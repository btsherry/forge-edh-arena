# Bug log — outstanding items

Opened 2026-09-04 after the full review (`docs/reviews/2026-09-03-full-review.md`), the affirmed interactive plan (`docs/reviews/2026-09-03-interactive-plan.md`, items 1–15 shipped, 16 measuring) and live game 19. Extended the same day from the blind round-two review (`docs/reviews/2026-09-04-round-two-comparison.md`): BL-18..28, W-5, HL-13..20, and the BL-16 list. Every entry is something NOT yet fixed. Severity: **P0** corrupts state/data or a batch; **P1** wrong behaviour in play or measurement; **P2** minor or latent. Status is the only field that changes; close an entry by moving it to the "Closed" section with the commit.

Conventions still in force: seam fixes are general (mechanics, never card names); regression tests span ≥2 cards; parent-module changes take the FULL gate; capture Maven's exit status, never a filtered tail.

## Interactive (Project 2) — open

| ID | Sev | Area | Item | Evidence | Proposed fix |
|---|---|---|---|---|---|
| BL-04 | P2 | mailbox surfaces | Combat damage assignment (multi-block splits), convoke/improvise payment, spell-for-effect choice stay on stock. | Game 19: `assignCombatDamage` 1×, convoke 9× (repeated planning consultations per window), spell-for-effect 0×. | Open by evidence after a few more games; convoke needs the payment-context gate (item 10's F2 lesson) so planning scans don't open windows. |
| BL-06 | P2 | mailbox | Timeout on a silent brain still costs one full wait per decision once (the heartbeat gate only helps when the runner PROCESS is gone). | Design decision (item 12 simplified); item 2 makes wedged models punt on time, so exposure is one window. | Leave unless measured; revisit with the surfaces above. |

- **BL-44** — **Selvala's any-colour mana pick costs one model call per mana point on AI seats (game 32, seat 0 under the Executive, t23).** Each Selvala activation opened ten CHOOSE_MODE windows ("Green." ×10, ~2.5 s each); two taps = twenty calls, ~50 s of a main phase. The human side already answers any-colour mana picks in the commander's colour for mono-coloured commanders (BL-31). Proposed seat-side rule, general: when the deck's colour identity is one colour, answer a mana colour pick with it without a call; for multi-colour decks, replay the model's first pick for the SAME activation (same turn, same prompt, same option set) for the rest of that activation. Runner Python only. Not built 2026-09-08 (observe pass).

- **BL-46** — **A "tap an untapped artifact you control" payment takes stock's pick; the seat cannot choose (game 38 — the v4.0 package acceptance game — seat 0 Urza, t10 and t14).** Urza's own mana ability (tap an untapped artifact: add {U}) was paid by tapping Winter Orb both times, loosening the seat's own untap lock for a round; the seat wanted the Construct token and said so in two DEVIATIONs. `TapCostPreference` exists but is fed only for the symmetry-break offers; every other `CostTapType` payment is stock's choice. Fix (same pattern as sacrifice/pitch payments): when a CostTapType has more than one candidate, ask the seat (CHOOSE_ENTITY) through the existing hook, vet-else-stock; forge-ai `AiCostDecision.visit(CostTapType)` already consults the hook, so the work is on the mailbox side (populate the preference from a seat window before the activation pays). Found 2026-09-09 22:05. Post-4.0 (4.1).

## Interactive — watch items (not bugs yet)

- **W-1** Surface-count instrumentation (`STOCK-SURFACE`) counts engine consultations; convoke and color are consulted many times per window. De-duplicate by (seat, turn, phase, surface) before comparing.
- **W-2** A resumed Claude session hung three times in a row (game 19, seat 1, 72 s each) before the wedge rule fired. Three punts is ~3.6 minutes of stock play at 90 s. If it recurs, lower `WEDGE_FAILS` to 2 or shorten the second attempt.
- **W-3** Ratings void any game with a wedge; with wedges now cheap (item 2), consider voiding only when punts exceed the threshold.
- **W-4** `manaAvailableNow` counts one activation per source; the brain still misjudged Gemstone Caverns once (t42) — confirm the `restricted`/condition flags read right for condition-forked sources.
- **W-6** Two colour paths can open a COLOR window mid-payment by design of the Mana-api bypass (review 2026-09-04): `ManaEffect.handleSpecialMana` for EnchantedManaCost / EachColoredManaSymbol producers (Elemental Resonance, Charmed Pendant — neither in a shipped deck) and `specifyManaCombo` once per mana for a multi-mana combo source when the brain floats it deliberately. Watch the STOCK-SURFACE/COLOR counts; if a shipped deck hits it, narrow the bypass to Amount 1.
- **W-7** `chooseColors` with `min = 0` ("UpTo" colour effects, no shipped script uses one) cannot answer "no colour" except through the hand-back to stock.
- **W-8** REACT windows on an opponent's turn can offer an instant the seat cannot pay for (game 21, Giada t19/t20: Teferi's Protection offered at `manaAvailableNow` 0, needed 3). The refusal guard caught the one attempt (REFUSED, card kept in zone) and the brain then reasoned around it on its own, so nothing broke — but the phantom option costs a decision and a re-plan each time. Check whether the REACT option builder applies the same affordability filter as CAST_SPELL before offering.
- **W-9** Which artifact Urza's mana ability taps is chosen by the engine's payer, not the seat (game 22, Urza t15: the brain floated one {U} and the payer tapped Everflowing Chalice, worth {C}{C}, while a 1-mana artifact was untapped; the seat's own deviation log caught it — "leaving only 4 available" for the {5} ability). A cost-choice surface ("tap an untapped artifact you control") with a value difference between candidates; check whether `CostTapType` choices reach the mailbox or fall to `ComputerUtil.chooseTapType`.
- **W-12** Opponents' static permanents reach the seat by NAME only; the model must recall their text. Game 29 t22: Urza cast Nexus of Fate for an extra turn under Giada's Trouble in Pairs (on the board since t9) — DEVIATION. Candidate: oracle text for opponents' non-land permanents with static/replacement effects, length-capped, the way `stackOracle` grounds stack items. Weigh against per-request token cost (every request carries it) before building.
- **W-11** Payer spent a general source (Plains) on a pip a restricted-but-eligible source (Cavern of Souls, chosen type Angel) could have paid, stranding the follow-up. Game 29 t9, Giada seq 10–13: Wojek Investigator IS an Angel (Creature — Angel Detective; Ben caught my mis-read on 2026-09-08), so the floated Angel-only {W} legally paid its {W}; for the {2} the payer took Dust Bowl (colorless-first) and then the PLAINS rather than Cavern of Souls — Forge's source order puts any-colour lands last — leaving only Cavern, whose mana cannot cast the non-Angel Mother of Runes. Not a model error. Candidate fix (forge-ai, general MTG principle "spend the least flexible mana first"): when the spell being paid satisfies a source's RestrictValid / Cavern-type restriction, order that source BEFORE unrestricted lands of the same colour. Also staged: `poolDetail` (floating mana with its restriction) for grounding.
- **W-10** `chooseColors(1-1)` fell to stock twice for Purphoros (game 22, t21 and t25) with no window reaching the seat — the payment-context gate, i.e. a "choose a color" made while paying a cost (Nykthos, Shrine to Nyx is on that board; its ability chooses a colour inside a Mana-api resolution the payer drives). Mono-red makes stock's pick right, so no harm here; a multi-colour deck with a Nykthos-class producer would have the engine choose. Check which `SpellAbility` reaches `chooseColors` for payer-activated mana abilities (W-6 lists the two known bypass paths; this may be a third). Also seen: `chooseColor` ×9 for Urza's Moxen during payment (by design, mono-blue) and `chooseSomeType(Creature)` ×2 (BL-04 class).
- **W-5** Heartbeat liveness means "process alive", not "loop can answer": through a 60–90 s pre-warm or a crash-restart loop the engine waits the full timeout per decision. This is the simplified item-12 design Ben affirmed; measure before changing (round two, Fable C B10).

- **W-13** — **One 72 s persistent-call timeout (game 31, seat 2 Purphoros, t21 MAIN1 CAST_SPELL, 14:07:38).** The process was killed, the window was spent ("no spawn retry" by design), the seat punted once and a new persistent process answered the next call in 5 s; the other three seats answered normally in the same minute, so not an API-wide stall. First punt in five games on the branch. Watch: if a second single-call stall appears, log the prompt size and consider a mid-call progress signal from the stream-json reader (the persistent process streams; a call that emits nothing for 30 s could be killed early and retried in the remaining window).

- **W-14** — **No window over the seat's own ACTIVATED ability (game 31, seat 0 Urza, t30: Lithoform Engine could not copy Manifold Key's activation).** By design (`MailboxController` own-main gate, 2026-08-10): with the stack holding only the seat's own non-trigger objects the window is a clean pass, so a seat never holds priority over its own activation. Triggers (`selfTrigger`) DO open a window, so Rings of Brighthearth-style copy triggers work; only "activate X, then activate Y targeting X's ability on the stack" (Lithoform Engine, Strionic Resonator on abilities) is unreachable. Rare line, deliberate flood trade-off; the deviation is honest. Watch: if it recurs, open the own-activation window ONLY when the seat controls an untapped permanent whose activated ability targets a spell or ability on the stack (Lithoform, Resonator, Twincast-class), detected from the target restriction, never by name.

- **W-15** — **The payer spent every white source on a commander recast's GENERIC part while colorless-only sources stayed untapped (game 32, seat 2 Giada, t21).** Board: Cavern of Souls (Angel), three untapped Plains, Mana Vault (cast that turn), Urza's Saga. Recast Giada ({1}{W} + {2} tax): the payer tapped Cavern + all Plains and left Vault ({C}{C}{C}) and Saga ({C}) untapped; Exemplar of Light (needs {W}) became uncastable — DEVIATION "White sources all tapped". Cavern-first is right (W-11: restricted-eligible first for the Angel); the Plains-over-Vault/Saga choice for the generic shards is the question. Read 15:40: `ComputerUtilMana.sortManaAbilities` orders sources by `scoreManaProducingCard` (lower first) — basics score lowest and are tapped first; a drawback source (Mana Vault: does not untap, damage) scores high and is kept for last; the only cross-source colour thinking is a tie-break among EQUAL scores ("keep access to more colours"). So stock spent the Plains on purpose. Two remedies, neither built: (a) brief text — "when a later spell this turn needs a colour, FLOAT the colorless sources first, then cast" (the float options exist in main phase; the seat's own plan said "7 mana covers both" and never sequenced it); (b) payer: for generic shards prefer sources that can pay no coloured pip of any card in hand. Fix, if built: for generic shards prefer sources that can pay NO coloured shard of any castable card in hand (colorless-only first), then the rest — general rule, no names. forge-ai → FULL gate.

- **W-16** — **Cavern of Souls is summed as restricted only (game 37, seat 1 Urza, t19 upkeep).** `manaSources` keeps ONE ability per permanent (the bare-tap one, else the first); Cavern has two bare-tap abilities — {C} unrestricted and the creature-type-restricted colour — and the row took the restricted one, so `manaAvailableNow` read 4 with five untapped sources and the affordability fastpath passed a REACT window ("cheapest option costs 5, 4 mana available now") that Cavern's {C} would have funded; the payer then paid Pact's {3}{U}{U} from four Islands + Cavern's {C} without trouble. Fix (small, Java): when a permanent has several bare-tap abilities prefer an UNRESTRICTED one for the row, and note the restricted colour in a `also` field. `ManaTableTest`; standard gate. Not built 2026-09-09.

## Headless harness (Project 1) — deferred by decision 2026-09-03, recorded for reference

Ben removed these from planning. Listed so they are not lost; details and line numbers in `docs/reviews/2026-09-03-full-review.md` and the reviewer reports it cites.

| ID | Sev | Item |
|---|---|---|
| HL-01 | P0 | `EngineFacade` wall-clock timeout calls `setGameOver` from the harness thread while the game thread still runs; the abandoned 64 MB thread writes into a closed recorder for the worker's life. |
| HL-02 | P0 | `ArenaRunner.runOne` lets setup exceptions escape: no `GameRecord`, recorder leaked, worker killed, stride lost after 3 identical retries. |
| HL-03 | P0 | 53 combo programs, 18 pairing programs, 1 engine program are gitignored (`decks/*/`); 25 tests error on a clean checkout, `ProgramSchemaValidationTest` passes vacuously. Track the authored inputs. |
| HL-04 | P0 | Every Java-prepped dossier fails `DossierCheck` (`deck_cards` sha mismatch, caused by `arena-add-deck.py` overwriting). Two ingestion writers → one. |
| HL-05 | P1 | `ComboAwareLobbyPlayer.getAi()` returns a second `AiController`; 62 upstream call sites consult a brain that is not deciding; card memory never reset. |
| HL-06 | P1 | Nine of eleven program-dispatch branches `return first` on a null first step, forfeiting the priority window. |
| HL-07 | P1 | `PairedPlay` decides "land" from an 11-name basic-land list; land wipes score near zero against real manabases. |
| HL-08 | P1 | `PayoffRules` matches Clue reminder text and symmetric draws: Loran, Wojek Investigator, Minas Tirith shipped as draw engines. |
| HL-09 | P1 | `firedShortcuts` doubles as "pool banked" and "combo fired"; `convert()` clears it for every combo on phase change. |
| HL-10 | P1 | `SelvalaManaLoopRunner` embeds ten card names and a Rhonas/Nylea plan; `amplified()`/`perEtbDamage()` parse oracle prose; `PairingRunner` pip check is white-only. |
| HL-11 | P1 | `ExecutorBindings.executorFor` throws out of eight unguarded call sites on a known archetype with a missing param; `BindGen` lints every archetype as `TapForManaUntapLoop`; bindings/route library rewritten non-atomically. |
| HL-12 | P2 | `BatchMain` exits 0 on 100% crashes; stall dumps CWD-relative with 32-bit hash names; dead `chooseCardsForZoneChange` override; `confirmAction` lacks `@Override`; `StallAutopsy` deck gate is a substring test and reads an arbitrary `dump_path`. |
| HL-13 | P1 | Combo funnel counts program *dispatches* as fires (`combo_shortcut` before a runner exists) and `BatchStats` increments `shortcutGames`/`eventualWinAfterFire`/`sameTurnConversions` per event with no per-game dedupe; a deck whose programs are all unreadable passes `PilotFloors` at 100 %. (round two) |
| HL-14 | P1 | `pooledManaStillLive` compares `bankedPhase` to a phase *name* and `convert()` only runs in MAIN1, so `pool_expired` is dead code and banked-pool state survives across turns; program dispatch sets `bankedPhase`/`currentRoute` with no pool injected. Deepens HL-09. (round two) |
| HL-15 | P1 | `arena-add-deck.py` writes `combos.json`/`deck-cards.json` that violate the schemas they tag (`spellbook_snapshot` int, no `deck_hash`, `mana_needed` null, no `zone_req`; `layout`/`scryfall_id` under `additionalProperties: false`); `TutorWeights` NPEs on `deck_hash`. Verified on sheoldred. Deepens HL-04. (round two) |
| HL-16 | P2 | `Map.of` in `GameEventBridge:138` and two `ComboPilot` sites: per-JVM salted iteration order breaks cross-process byte determinism; `SeedDeterminismTest` is single-JVM. (round two) |
| HL-17 | P2 | Worker stride equals the latin-square offset with default counts; a worker dead after three respawns removes one whole seating arrangement and `summarize()` does not warn. (round two) |
| HL-18 | P2 | `ProgramGate.deriveFixture` yields `""` card names for template pieces (six Urza programs) → `flagged engine_crash: NullPointerException` verdicts that blame the engine. (round two) |
| HL-19 | P2 | Events schema's closed `t` enum omits ~12 emitted types (`pool_expired`, `program_abort`, `engine_cycle`, `pairing_*`, …); `ComboPilotTest` validates every event, which is one reason those paths have no tests. (round two) |
| HL-20 | P2 | Four mana-cost parsers with three X policies; `ProgramRunner` swallows its own parse failure with no event; `activateAtOpponent` returns false without `lastFailure`; `EventRecorder.close` unsynchronized (zombie thread writes after `game_end`, breaks the canary assert). (round two) |
| HL-21 | P2 | headless combo scenarios | Five seeded scenario games (GiadaDoomskarPairTest, GiadaArmageddonPairTest, UrzaScepterLoopTest, UrzaTidespoutLoopTest ×2) passed on `arena` and fail after the 2026-09-10 upstream sync: under the new engine (≈100 lines of attack/block evaluation rewritten upstream, new scripts for fixture cards incl. Urza's Saga) the games unfold differently and end on the pilot's own continuous lethal-alpha check before the asserted program stage, or the governor plans another line. Not a silent revert (every behavioural-patch test passed). Fenced into TestNG group `headless-scenario`, excluded by default AND from the FULL gate; run with `-Darena.excluded.groups=none` when re-baselining. Re-baseline = Project 1 work (fixtures/expectations vs the new AI). | sync 2026-09-10 | open |

## Closed 2026-09-08 (hardening pass, `experimental/voicework` 8f14c8f8349)

- **BL-45** — **A planeswalker's −X loyalty ability was capped by the MANA X-ceiling (game 37, seat 1 Urza, t27 MAIN2).** Tezzeret the Seeker at 4 loyalty, −X: the CHOOSE_NUMBER window said "Your affordable X RIGHT NOW is 0 — you have floated no mana"; the seat believed it, chose X=0 and fetched Seat of the Synod instead of The One Ring at X=4 — two DEVIATIONs. Item 6's X ceiling (pool + untapped sources) is right for X in a MANA cost; a loyalty X is paid with counters and its ceiling is the walker's current loyalty. Fix: detect the cost's X kind (mana shard X vs loyalty/counter X) and cap + word the hint accordingly; when the engine's own min/max already bound it, never lower the max below that. FIXED 1fd92b44a36: `xPaidWithMana(Cost)` decides; a mana X keeps the ceiling and hint, any other X keeps the engine's bounds with a hint that names the kind (`state.xKind`). `ManaTableTest.loyaltyXIsNotAManaX`. FULL gate 467/467 green 2026-09-09 01:45.

- **BL-43** — **A seat reads `manaAvailableNow` as its ceiling in reactive windows and misses lines the payer could fund (game 31, seat 3 Selvala, t22 declare-blockers).** With Rhonas's {2}{G} pump offered, 2 plain mana untapped and Selvala untapped ({G},{T}: add 8), the brain passed: "Only 2 mana left, Rhonas costs 3; Selvala's ability isn't offered here" — DEVIATION. Facts: (1) reactive/tactical windows drop ALL mana abilities from the option list by design (a mana ability is never the action); (2) `manaSources` lists Selvala with `yield 8, cost {G}` but `manaAvailableNow` sums only bare-tap, unrestricted, non-sick sources, so it read 2; (3) `canPayCost(Rhonas)` was true — the engine's payer would have tapped Selvala for it. The brain had no line telling it that. Proposed fix (grounding, no rule change): add `manaReach` to the state = `manaAvailableNow` + the net yield of every costed, non-restricted, non-sick, non-dormant source whose extra cost is payable from the summed mana (Selvala {G} → 8, net +7; Nykthos {2} → devotion), and one sentence in the seat brief: "an offered option may cost more than manaAvailableNow — the payer taps sequence sources (Selvala, Nykthos) for you; manaReach is that ceiling". Java (`MailboxController.manaAvailableNow` sibling) + brief + `ManaTableTest`; FULL gate. BUILT 2026-09-08 (6460a521d38 + fixture refresh): `manaSources` rows carry `costMana` for mana-and-tap-only extra costs; `state.manaReach`; seat brief sentence; the runner's affordability fastpath uses the reach (it had the same blind spot). `ManaTableTest.manaReachFundsSelvalaFromPlainMana` + `manaReachIgnoresSequencesWithNonManaCosts`, `test_hardening.ManaReachCeiling`. FULL gate: the only two failures were this test's sick Selvala and the protocol fixtures gaining the key; both fixed, re-run green.

- **BL-39** — **A seat runner restarted mid-game lost the game.** The restart loop gave the new process a fresh session with the dossier only; every earlier decision and reason was gone (the rotation record existed but only rotation used it). Fix: `SeatRunner._restart_record` + `SeatBrain.ensure_session(record_text=…)` — dossier + record + READY on restart, mailbox game id seeded so a different game still resets; `restart` transport event. `test_hardening.RestartKeepsTheRecord`.
- **BL-40** — **`--stops` overwrote its own backup on every launch (latent).** The 09-08 morning `--stops quick|full` copied `forge.preferences` to `.bak-arena` unconditionally, so the second launch would have saved the already-rewritten file over Ben's originals. Fix: the backup is written only when absent; `--stops keep|restore` added; quick is the human-game default; spectator games never touch preferences. `test_hardening.StopsRestore`.
- **BL-41** — **A spent ElevenLabs quota logged one failure per line.** Fix: after three consecutive failures live renders pause 60 s, doubling to 10 min, one line per pause; recovery logged; events in `voice-0.jsonl`. `test_hardening.VoiceBackoff`.
- **BL-42** — **The packager would have shipped a launch that cannot start (latent).** `build-light-package.sh` copies scripts by an explicit list; `arena-config.py`, `arena-hygiene.py` (called by arena-play/arena-stop) and `arena-public-state.py` (the advisor's tool) were not on it, and the runner rsync carried `voice/stock/raw/` (4 MB of raw takes) and `sfx/legacy/`. Fix: list + excludes; `test_hardening.PackagerShipsWhatTheScriptsCall` derives the needed set from the shipped scripts' `$DIR/…` references and fails on drift.
- **Decision** — only decks Ben built ship. The three 09-06 random bracket-3 test imports (Pantlaza, Syr Gwyn, Thalia + Gitrog) removed with dossiers and primers; the packager's ten decks unchanged.

## Closed 2026-09-07 (game 24 findings)

- **BL-38** — **Counter-removal costs failed silently and the seat retried blind (Scholar of New Horizons).** Game 30 t3 and t7: Giada chose Scholar's activation ({T}, remove a counter from a permanent you control); Forge's `AiCostDecision.visit(CostRemoveAnyCounter)` removes only counters its heuristics call harmless (persist/undying, negative, useless, quest, lore) and returned null when the only counter was Scholar's own +1/+1 → "AI failed to play … rolling back", no refusal reached the seat, which chose it again. Fix: (1) the decision consults the seat's `PaymentPickPreference` (new kind REMOVE COUNTER) before giving up — forced when one permanent qualifies, else a CHOOSE window, like sacrifice/exile/discard payments; (2) any failed play now leaves a refusal note ("the engine could not pay its cost or set its targets") so the next request says so and omits the card for one window. forge-ai touched → FULL gate.
- **BL-36** — **Sub-ability targets were never asked of the seat (Bridgeworks Battle's "up to one target creature you don't control").** Game 29 t19: Selvala chose the pump target, the fight sub-ability (TargetMin 0, a SUB-ability) got no window, the fight had no target, Serra Paragon lived — DEVIATION. The mailbox cast path pre-targeted only the root ability; Forge's AI cast path never runs the target chooser for sub-abilities. Fix: `playChosenSpellAbility` walks the chain and pre-targets every targeting part like the root (single-target → the seat, with "No target (decline)" when optional; multi-target → stock; a required part with no answer keeps the card in hand). Gate pending at time of writing.
- **BL-37** — **No way to tap a land on purpose for an untap line (Arbor Elf + Forest).** Game 29 t7: Selvala planned tap Forest → Arbor Elf untaps it → tap again for the third mana; bare land taps are hidden from the option list, so the line was impossible — DEVIATION. Fix: when the seat controls an untapped, non-sick permanent with an activated Untap ability targeting lands (`landUntapperReady`), ONE float option per land name is offered, labelled as the enabler; otherwise unchanged (no flooding). `ManaTableTest.landUntapperReadyDetectsArborElf`.
- **BL-35** — **Condition-locked mana sources counted as available (Mox Opal without metalcraft).** Games 27 t2 and 28 t1: Urza planned Etherium Sculptor around a dormant Mox Opal; the payer refused → DEVIATION both games. `manaSources` listed the ability with yield 1 and `manaAvailableNow` summed it. Fix: rows whose ability restriction fails now carry `dormant: true`; `manaAvailableNow` and the human `manaCeiling` skip them. Kit test with one vs three artifacts. Gate 398 green.
- **BL-34** — **Every seat decision re-read the entire game transcript (Claude `--resume`): 61k → 807k cached tokens per call by t23 in game 25, 95M per seat per game; two session-limit incidents trace to this.** Design since 08-07 (one session per seat per game), surfaced by trigger-heavy boards. Fix: notes 81 (rotation with the machine-built record) + 79/80 (fewer calls). Open follow-ups: persistent process per seat; low effort for unthreatened REACT; engine-side yield mirror (HOW-IT-RUNS.md).
- **BL-33** — **The autopass table passed the human out of real windows (Staff of Domination) and the seats paid ~4 s for hundreds of repeat REACT offers.** Game 25: 54 "only utility activations available: Staff of Domination" receipts t15–19 — a {5} draw with mana up is a play; on the seat side 84 windows in one combo turn, Giada asked 68× about a tap-only One Ring with zero mana, Purphoros 103× about Flare of Duplication with only abilities on the stack. Fix: note 79 (mana-costing abilities are plays; spell-reactor dead rule; repeat-with-life-guard; life-bucketed memo). Replayed on game 25's logs: 0 windows the model had acted on would be cut. The window VOLUME is board-driven and unchanged (see [[parked-ai-snappiness]] for the per-call lever).
- **BL-32** — **Imprinted / exiled-with cards and the other public zones were invisible to the seats, the observer snapshot and the advisor.** Game 25 (Ben: "shouldn't the imprint on Isochron Scepter be public knowledge?"): a permanent's `cardState` listed attachments but not what it imprinted or exiled (CR 406.3 makes those face up and public), and the snapshot carried only battlefields and stack names. Fix: `imprinted` / `exiledWith` on every permanent (seat requests and snapshot), per-seat `graveyard` / `exile` / `commandZone` (face-down exile stays "(face-down card)"), `stackDetail` with owner and targets; `scripts/arena-public-state.py` renders it and is the advisor's one allowed tool (note 78). `ObserverSnapshotWriteTest.publicZonesAndImprintsAreInTheSnapshot`, `runner/tests/test_public_state_tool.py` (6).
- **BL-31** — **Casts-mode autopass kept far too many no-op prompts open.** Game 24 (Ben on Selvala): 51 PRIORITY prompts reached the human against 50 auto-passes, most with nothing affordable to do. Three causes: Forge's `AvailableActions` scan is affordability-blind (a Spree instant at 1 Forest counts as an action) and time-boxed (fails open); our layer kept EVERY stop open while an opponent's spell or trigger was on the stack (every Purphoros ping = a prompt); Forge's stop settings enable four stops per opponent turn. Fix (Ben: "more aggressive when I have no mana and no plays, or am just acknowledging damage pings"): `AutopassPolicy` — a pure decision table the controller feeds with the live board: floating mana / declare steps / own mains / fresh equipment keep (unchanged, sacred); a free play keeps; no real play passes even with an opponent item on the stack ("nothing to respond with"); real plays whose cheapest cost exceeds the mana ceiling (pool + every untapped source's yield from the seats' own mana table) pass with the numbers; an unknown ceiling (Selvala-class yield) stays conservative. Kept prompts outside the sacred stops leave a receipt naming the card that held them open. Ben's follow-up ("any concerns about responding with tapping a creature or another permanent?"): three response-aware keeps ahead of the utility-only pass — a utility whose permanent is TARGETED by the opponent's stack item, a utility with a SACRIFICE cost while an opponent's item is up, a TARGETING utility at an opponent's begin-combat (tappers, Maze of Ith); plain utility (Arbor Elf's untap) under a Purphoros ping still passes. Log-driven additions (game 24: 161 answered prompts, 82 own-turn / 74 opponents'): declare steps pass when NOTHING is possible (no free/affordable play with a known ceiling, no activated ability); own-spell resolve in the main phase behind `ARENA_AUTOPASS_RESOLVE_OWN=on` (off by default — the one hole in "mains are sacred"). Rejected as not low-risk: passing a repeat stop in the same phase (a second trigger may target something else). Wedge threshold deliberately unchanged (Ben). `AutopassPolicyTest` (7).

## Closed 2026-09-06 (game 23 findings)

- **BL-29** — **Chat field fired Forge's match hotkeys while typing.** T/S/C/Y/N/P/L/D are bound at `WHEN_IN_FOCUSED_WINDOW`; a JTextField does not consume the KEY_PRESSED of a typed letter. Game 23: one question at t2 with odd counts of t and y turned the targeting arrows off and set an auto-yield ("arrows aren't coming up like normal"). Fix: `HotkeyGuard.swallow` consumes the KEY_PRESSED of printable unmodified keys in the field (KEY_TYPED still inserts; Enter/Backspace/Escape/arrows/chords untouched); Escape leaves the field; a send returns focus to the game. `HotkeyGuardTest` (4).
- **BL-30** — **A seat's own MODAL trigger with a targeted mode lost its target.** `prepareTriggerViaSeat` returned right after `CharmEffect.makeChoices` for every non-Random modal trigger, so the seat's chosen mode reached the stack untargeted and Forge dropped it (`[Couldn't add to stack, failed to target]`; game 23 t21, Kogla and Yidaro's fight on Syr Gwyn). The triggered-ability twin of the 2026-08-17 Charm-spell mode-discard fix. Fix: the chosen mode chain goes through the same seat aiming as any other own trigger (CHOOSE_ENTITY; silent brain → stock aims the CHOSEN mode, never CharmAi's re-pick); a mode without targets stacks as before. `ModalTriggerAimTest` (Kogla and Yidaro fight, Charming Prince exile, silent-brain fallback).

## Closed 2026-09-04 (round-two cleanup, `docs/reviews/2026-09-04-interactive-cleanup-plan.md`)

Commits: 91cc85d9292 (P), 2bc78d92188 (J1), d1a03910f25 (J2 payer), c2e7e5ee1da (docs), 1fb8413ebcb (ratings dedupe), 7bf14c2fcad (review follow-ups), 95e8c34c18a (extended tests), 51e98b931f4. Standard gate green (363 tests), FULL gate green with the `extended` group (429 arena tests + parent modules). Live validation: game 20 (2026-09-04 02:50) was invalidated by the Claude subscription's session limit — every seat's model call failed from 02:56 and the game ran on punts; a validation game is re-run after the limit resets and its findings, if any, reopen items here.

- **BL-17** — **v3.3.3 cut and shipped 2026-09-04.** FULL gate green on the review-fix tree (`14cd9b8b219`: 429 arena + 286 parent tests), packaged from `5f72c0a8714`, packager negative checks clean (no `dossier/.cache`, no DeckCheck creator payloads, retired scripts absent), `forge-light-llm-20260904.tar.gz` (118634974 bytes, sha256 `56bb675d…3bb12f`) PUT as dated + `-latest`, both hash-verified from the public URL. Live validation: game 21 (22 model turns, the new windows exercised) — see the plan's execution log.
- **BL-01** — **Life-for-mana statics are outside the stock payer's search.** With K'rrik, Son of Yawgmoth out, Mikaeus ({3}{B}{B}{B}) for 3 mana + 6 life…
- **BL-02** — **Trigger ordering is decided by stock** for the seat's simultaneous triggers (CR 603.3b gives it to the controller).…
- **BL-03** — **Any-color mana picks are decided by stock** (`chooseColor` / `chooseColorAllowColorless`).…
- **BL-05** — Mind-slave routing is untested live and the master's brain gets no `state` for its own board while controlling.…
- **BL-07** — `ObserverSnapshot` debounce has no trailing edge (last event in a 200 ms burst never written) and `write` is not synchronized.…
- **BL-08** — `_cycle_rebind` rebinds a recorded cycle answer by card-name prefix; when only one of a card's abilities carries a cost, the prefix can bind…
- **BL-09** — `transport-events.jsonl` is archived at teardown while the ratings void check reads it; multi-game sweeps can miss history.…
- **BL-10** — `status.py`'s fastpath % omits `hold`/`plan`/`cycle`; `usage_report.py` and `replay.py` index keys that pre-backend records lack; `arena-dig…
- **BL-11** — Ten seam tests still pin one card each (Fierce Guardianship, Transmute Artifact, Rings, Scepter, Aura Shards, Sanctum Weaver+Gauntlets, Selv…
- **BL-12** — No test exercises `GuiPilotMatch` (start invariant, roster, advisor wiring) or the human seat (`--human`, autopass modes, advisor-by-default…
- **BL-13** — `pending_context` is unbounded between admitted calls; the advisor writes its own control file then trusts its own mtime.…
- **BL-14** — `arena-add-deck.py` regex takes a leading number as quantity ("1996 World Champion"); Moxfield temp file leaks in `PrepMain`; DFC front-face…
- **BL-15** — `react-autopass.py` remains in the repo with a stale two-name allowlist and no threat check (no longer shipped or reaped).…
- **BL-16** — `docs/INVENTORY.md` §3 still describes `engine/` as three classes; ~50 files under `docs/` are not inventoried; `AiTabHarness` JUnit convers…
- **BL-18** — **DeckCheck API payloads ship in the public package.** `build-light-package.sh` rsyncs `dossier/.cache/` per deck; `forge-light-llm-20260901…
- **BL-19** — **A punt answers legally instead of yielding to stock.** `rules.safe_default` gives no blocks / no attackers / first `min` ids / keep / pass…
- **BL-20** — Cycle replay appends every answer to `_hist`, punts included, and `_cycle_try_arm` does not refuse a punt; a punted CONFIRM inside a loop re…
- **BL-21** — Item 13h collateral: `arena-digest.py` opens `game.jsonl` once and the symlink is re-pointed per game, so the digest goes silent after game …
- **BL-22** — `MailboxProtocol`'s constructor never sweeps `outbox/`; after a JVM crash leaves `resp-1.json`, a hand relaunch (bypassing `arena-stop`) let…
- **BL-23** — `rules.validate` hard-rejects a missing `defender`; `chooseDefender` accepts it with exactly one legal defender. With one opponent left a Ja…
- **BL-24** — `claude -p` runs with `cwd` at the repo root and default settings: every call loads `/Users/toor/Claude/CLAUDE.md`, project settings and use…
- **BL-25** — `ProtocolContractTest` writes ten tracked fixtures (dirty tree on drift; throws on a read-only checkout); the child surefire `<argLine>` rep…
- **BL-26** — `ratings.sweep` globs before it locks and renames a path a concurrent sweeper may have moved; `process_spool` indexes spool keys directly. A…
- **BL-27** — `extract_json` slices first `{` to last `}` (mana symbols in prose defeat it; plausible); whitespace in a deck name word-splits in three scr…
- **BL-28** — `gameId` is millis+pid, so two games in one millisecond in one JVM collide; the runner has no SIGTERM handler, so teardown orphans one in-fl…

## Closed (this pass, 2026-09-03 → 09-04)

Items 1–15 of the interactive plan, with commits: b0568854eb0 (1), a680583b7f4 (2), bb1917137f2/9dc20db1101 (3, 10), be668e60519 (4, 11c), 7490eb465dc (6), 04a12be8b98 (7), a783291a475 + ec2b1ede0b4 (8), 0597c40f023 (9), cde7095c2dc (11a/b, 12), b66d90ec75a (12 runner), 203d30ffe46 (5), 696c226a86f (13), ecd3bacbfe0 (14), 309bbe6d977 (15), d4e98dab13c (16 instrumentation). Live-found and fixed the same day: `getScryfallCode()` NPE in the resolver; resolver failure misreported as "Forge lacks the card"; MULLIGAN `state.hand` shape drift; eliminated seats read as CARD-VANISH (CR 800.4a).
